name: CI

on:
  push:
    branches: [ master ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
#       - name: Node.js 환경 설치
#         uses: actions/setup-node@v2.1.4
#         with:
#           node-version: 14.14
          
      - name: Next.js 빌드 파일 생성
        run: echo 안녕
